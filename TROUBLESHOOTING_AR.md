# 🔧 حل المشاكل الشائعة

## 🚨 خطأ: "Unexpected token '<'"

### السبب
الـ API يرجع HTML بدل JSON (عادة صفحة خطأ PHP)

### الحل

#### 1. افتح debug.php
```
https://yoursite.com/debug.php
```
شوف إذا كل الفحوصات ✅

#### 2. تحقق من GD Library
إذا ظهر ❌ عند GD Library:
- تواصل مع دعم الاستضافة
- اطلب تفعيل PHP GD Extension

#### 3. تحقق من الصور
تأكد أن:
- `templates/ar.jpg` موجود
- `templates/en.jpg` موجود
- الصور ليست تالفة

#### 4. تحقق من config.php
افتح `config.php` وتأكد أنه صحيح بدون أخطاء syntax

#### 5. شوف error logs
في لوحة التحكم:
- ابحث عن "Error Logs"
- شوف آخر الأخطاء
- اقرأ الرسالة بالكامل

---

## 🚨 خطأ: "Template not found"

### السبب
الصور غير موجودة أو المسار خاطئ

### الحل

#### 1. تأكد من رفع المجلد
```
templates/
  ├── ar.jpg
  └── en.jpg
```

#### 2. تحقق من الأسماء
- الأسماء يجب أن تكون بالضبط: `ar.jpg` و `en.jpg`
- حروف صغيرة (lowercase)
- امتداد `.jpg` وليس `.jpeg`

#### 3. تحقق من الصلاحيات
الملفات يجب أن تكون قابلة للقراءة (readable)

---

## 🚨 خطأ: "GD Library not found"

### السبب
PHP GD Extension غير مثبتة

### الحل

#### على استضافة مشتركة:
1. افتح لوحة التحكم (cPanel)
2. ابحث عن "Select PHP Version"
3. فعّل extension: `gd`
4. احفظ التغييرات

#### على VPS/Dedicated:
```bash
# Ubuntu/Debian
sudo apt-get install php-gd
sudo service apache2 restart

# CentOS/RHEL
sudo yum install php-gd
sudo service httpd restart
```

#### إذا لم تنجح:
تواصل مع دعم الاستضافة

---

## 🚨 خطأ: "CORS blocked"

### السبب
الاستضافة لا تسمح بـ CORS

### الحل

#### 1. تأكد من رفع .htaccess
الملف `.htaccess` يجب أن يكون موجود

#### 2. إذا لم ينجح، أضف في index.php
في بداية الملف، بعد `<?php`:
```php
header('Access-Control-Allow-Origin: *');
header('Access-Control-Allow-Methods: POST, OPTIONS');
header('Access-Control-Allow-Headers: Content-Type');
```

#### 3. إذا ما زال الخطأ موجود
بعض الاستضافات تمنع CORS تماماً. جرب:
- استضافة أخرى
- أو استخدم proxy

---

## 🚨 خطأ: "Failed to load certificate template"

### السبب
الصورة تالفة أو بصيغة خاطئة

### الحل

#### 1. تحقق من الصورة
- افتح الصورة على جهازك
- تأكد أنها تفتح بدون مشاكل

#### 2. تحقق من الصيغة
- يجب أن تكون JPEG
- ليست PNG أو غيرها

#### 3. أعد رفع الصورة
- احذف الصورة من السيرفر
- ارفعها مرة أخرى

#### 4. جرب تحويل الصيغة
إذا كانت PNG:
- افتحها في برنامج صور
- احفظها كـ JPEG
- ارفعها مرة أخرى

---

## 🚨 النص لا يظهر على الشهادة

### السبب
المواضع خاطئة أو الخط غير موجود

### الحل

#### 1. حمّل الخط العربي
```bash
php download_font.php
```

#### 2. عدّل المواضع
افتح `config.php` وغيّر الأرقام:
```php
'positions' => [
    'student_name' => [
        'y' => 105,  // ← جرب أرقام مختلفة
        'font_size' => 30,
    ],
    // ...
],
```

#### 3. جرب أرقام أكبر/أصغر
- زود الرقم = النص ينزل
- قلل الرقم = النص يطلع

---

## 🚨 الصورة ضبابية أو جودة سيئة

### السبب
جودة الصورة منخفضة

### الحل

#### 1. زود الجودة
في `config.php`:
```php
'image' => [
    'quality' => 95,  // ← زودها لـ 98 أو 100
],
```

#### 2. استخدم صور عالية الدقة
- الصور الأصلية يجب أن تكون HD
- على الأقل 1920x1080

---

## 🚨 الموقع بطيء جداً

### السبب
الصور كبيرة أو السيرفر بطيء

### الحل

#### 1. قلل حجم الصور
- استخدم أداة ضغط الصور
- حافظ على الجودة

#### 2. قلل جودة الإخراج
في `config.php`:
```php
'image' => [
    'quality' => 85,  // ← قللها من 95
],
```

#### 3. غيّر الاستضافة
بعض الاستضافات المجانية بطيئة

---

## 🚨 خطأ: "Missing required field"

### السبب
البيانات المرسلة ناقصة

### الحل

#### تأكد من إرسال كل الحقول:
```json
{
  "studentName": "أحمد محمد",     // ✅ مطلوب
  "courseName": "Flutter",         // ✅ مطلوب
  "instructorName": "د. محمد",     // ✅ مطلوب
  "date": "15 يناير 2026",         // ✅ مطلوب
  "language": "ar"                 // ✅ مطلوب (ar أو en)
}
```

---

## 🚨 خطأ: "Invalid JSON data"

### السبب
البيانات المرسلة ليست JSON صحيح

### الحل

#### في Flutter:
```dart
// ✅ صحيح
body: jsonEncode({
  'studentName': name,
  'courseName': course,
  // ...
}),

// ❌ خطأ
body: {
  'studentName': name,
  // ...
},
```

#### تأكد من:
- استخدام `jsonEncode()`
- Header: `Content-Type: application/json`

---

## 🚨 الخط العربي لا يظهر بشكل صحيح

### السبب
الخط غير موجود أو تالف

### الحل

#### 1. حمّل الخط
```bash
php download_font.php
```

#### 2. أو حمّله يدوياً
- اذهب إلى: https://fonts.google.com/specimen/Cairo
- حمّل الخط
- ضعه في `fonts/Cairo-Bold.ttf`

#### 3. تحقق من المسار
في `config.php`:
```php
'font' => [
    'path' => __DIR__ . '/fonts/Cairo-Bold.ttf',
],
```

---

## 🚨 خطأ 500 (Internal Server Error)

### السبب
خطأ في PHP

### الحل

#### 1. شوف error logs
في لوحة التحكم

#### 2. فعّل عرض الأخطاء مؤقتاً
في `index.php` (أول سطر بعد `<?php`):
```php
ini_set('display_errors', 1);
error_reporting(E_ALL);
```

⚠️ **مهم:** احذف هذا بعد حل المشكلة!

#### 3. تحقق من PHP version
يجب أن يكون 7.4 أو أحدث

---

## 🚨 خطأ 404 (Not Found)

### السبب
الملف غير موجود أو المسار خاطئ

### الحل

#### تأكد من:
- رفع `index.php`
- الرابط صحيح
- لا توجد مسافات في الرابط

---

## 📞 ما زلت تواجه مشاكل؟

### خطوات التشخيص:

1. **افتح debug.php**
   ```
   https://yoursite.com/debug.php
   ```

2. **شوف error logs**
   في لوحة التحكم

3. **جرب test.html**
   ```
   https://yoursite.com/test.html
   ```

4. **افتح Console**
   اضغط F12 في المتصفح

5. **اقرأ الرسالة كاملة**
   لا تتجاهل التفاصيل

### معلومات مفيدة للدعم:

عند طلب المساعدة، وفّر:
- رسالة الخطأ الكاملة
- نتيجة debug.php
- محتوى error logs
- إصدار PHP
- اسم الاستضافة

---

## ✅ نصائح للوقاية

1. **اختبر محلياً أولاً**
   قبل الرفع على الإنترنت

2. **احتفظ بـ backup**
   للملفات الأصلية

3. **راقب error logs**
   بشكل دوري

4. **حدّث PHP**
   استخدم أحدث إصدار مستقر

5. **استخدم debug.php**
   بعد أي تغيير

---

**بالتوفيق! 🚀**
