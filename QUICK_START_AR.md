# 🚀 دليل البدء السريع - مولد الشهادات

## ✅ الخطوة 1: اختبار محلي

### افتح Terminal في مجلد `certificate web`
```bash
cd "certificate web"
php -S localhost:8000
```

### افتح صفحة الفحص أولاً
```
http://localhost:8000/debug.php
```
هذه الصفحة ستفحص كل شيء وتخبرك إذا كان هناك مشاكل.

### إذا كل شيء ✅ افتح صفحة الاختبار
```
http://localhost:8000/test.html
```

### جرب توليد شهادة
- اكتب البيانات في النموذج
- اضغط "توليد الشهادة"
- يجب أن تظهر الشهادة مع البيانات

### إذا ظهر خطأ
- افتح Console في المتصفح (F12)
- شوف الرسالة الكاملة
- راجع `debug.php` للتأكد من كل شيء

---

## 📤 الخطوة 2: رفع على الإنترنت

### الطريقة السهلة: InfinityFree (مجاني)

1. **سجل حساب**
   - اذهب إلى: https://infinityfree.net
   - سجل حساب جديد

2. **أنشئ موقع**
   - اضغط "Create Account"
   - اختر اسم للموقع (مثال: `myapp-certificates`)
   - انتظر حتى يتم إنشاء الموقع

3. **ارفع الملفات**
   - اذهب إلى "File Manager"
   - افتح مجلد `htdocs`
   - احذف كل الملفات الموجودة
   - ارفع كل ملفات `certificate web`:
     - ✅ index.php
     - ✅ config.php
     - ✅ test.html
     - ✅ .htaccess
     - ✅ مجلد templates (مع الصور)
     - ✅ مجلد fonts

4. **اختبر الموقع**
   ```
   http://yoursite.infinityfreeapp.com/test.html
   ```

---

## 🔧 الخطوة 3: ربط مع التطبيق

### في Flutter، غير الرابط في الكود:

```dart
// في ملف certificate_screen.dart أو service جديد
static const String certificateApiUrl = 'https://yoursite.infinityfreeapp.com/index.php';

Future<String> generateCertificate() async {
  final response = await http.post(
    Uri.parse(certificateApiUrl),
    headers: {'Content-Type': 'application/json'},
    body: jsonEncode({
      'studentName': _studentName,
      'courseName': widget.course.title,
      'instructorName': widget.instructorName,
      'date': _completionDate,
      'language': isArabic ? 'ar' : 'en',
    }),
  );

  if (response.statusCode == 200) {
    final data = jsonDecode(response.body);
    return data['image']; // base64 image
  }
  
  throw Exception('Failed to generate certificate');
}
```

---

## 🎨 الخطوة 4: تعديل المواضع (إذا لزم الأمر)

إذا كانت النصوص في مكان خاطئ على الشهادة:

### افتح `config.php` وعدل:

```php
'positions' => [
    'student_name' => [
        'y' => 105,           // ← غير هذا الرقم لتحريك الاسم لأعلى/أسفل
        'font_size' => 30,    // ← غير حجم الخط
        'centered' => true,
    ],
    'course_name' => [
        'y' => 148,           // ← موضع اسم الدورة
        'font_size' => 16,
        'centered' => true,
    ],
    'date' => [
        'x' => 75,            // ← موضع التاريخ من اليسار
        'y_from_bottom' => 35, // ← موضع التاريخ من الأسفل
        'font_size' => 14,
    ],
    'instructor' => [
        'x_from_right' => 70, // ← موضع اسم المدرب من اليمين
        'y_from_bottom' => 35,
        'font_size' => 14,
    ],
],
```

---

## 🎯 نصائح مهمة

### ✅ تأكد من:
- [ ] رفع مجلد `templates` مع الصور
- [ ] الصور موجودة: `ar.jpg` و `en.jpg`
- [ ] رفع ملف `.htaccess` (مهم للـ CORS)
- [ ] الاستضافة تدعم PHP و GD Library

### 🔍 اختبار سريع:
```
افتح: https://yoursite.com/test.html
جرب توليد شهادة
إذا ظهرت الشهادة = كل شيء يعمل ✅
```

### 📱 في التطبيق:
- استخدم `http` package
- أرسل POST request للـ API
- استقبل الصورة base64
- اعرضها أو احفظها

---

## ❓ حل المشاكل

### المشكلة: "Template not found"
**الحل:** تأكد من رفع مجلد `templates` مع الصور

### المشكلة: "GD Library not found"
**الحل:** تواصل مع دعم الاستضافة لتفعيل GD

### المشكلة: CORS Error
**الحل:** تأكد من رفع ملف `.htaccess`

### المشكلة: النص في مكان خاطئ
**الحل:** عدل الأرقام في `config.php`

---

## 📞 تحتاج مساعدة؟

1. جرب `test.html` أولاً
2. تحقق من error logs في لوحة التحكم
3. تأكد من رفع كل الملفات
4. راجع `DEPLOYMENT_GUIDE.md` للتفاصيل

---

## 🎉 انتهيت؟

الآن التطبيق يمكنه:
- ✅ إرسال بيانات المستخدم للموقع
- ✅ استقبال الشهادة جاهزة
- ✅ عرضها أو حفظها
- ✅ كل شيء من التطبيق مباشرة!

**مبروك! 🎊**
