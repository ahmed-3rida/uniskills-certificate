# 🔧 حل مشكلة العربي والمواضع

## المشكلة 1: النص العربي يظهر رموز ❌

### السبب
الخط العربي غير موجود

### الحل السريع ⚡

#### الطريقة 1: تحميل تلقائي (موصى به)
```bash
cd "certificate web"
php download_font.php
```

#### الطريقة 2: تحميل يدوي
1. اذهب إلى: https://fonts.google.com/specimen/Cairo
2. اضغط "Download family"
3. افتح الملف المضغوط
4. انسخ `Cairo-Bold.ttf` إلى مجلد `fonts/`

#### الطريقة 3: استخدام خط آخر
إذا عندك خط عربي آخر (مثل Tajawal أو Almarai):
1. ضعه في مجلد `fonts/`
2. افتح `config.php`
3. غيّر المسار:
```php
'font' => [
    'path' => __DIR__ . '/fonts/اسم-الخط.ttf',
],
```

---

## المشكلة 2: النصوص في أماكن خاطئة ❌

### الحل التفاعلي ⚡ (الأسهل!)

#### افتح صفحة ضبط المواضع:
```
http://localhost:8000/adjust_positions.html
```
أو بعد الرفع:
```
https://yoursite.com/adjust_positions.html
```

#### خطوات الضبط:
1. **أدخل البيانات التجريبية** في الجانب الأيسر
2. **عدّل الأرقام** (Y, X, حجم الخط)
3. **اضغط "تحديث المعاينة"** لرؤية النتيجة
4. **كرر** حتى تصبح المواضع صحيحة
5. **اضغط "توليد الكود"**
6. **انسخ الكود** وضعه في `config.php`

---

## الحل اليدوي (إذا أردت)

### افتح `config.php` وعدّل:

```php
'positions' => [
    'student_name' => [
        'y' => 105,           // ← غيّر هذا الرقم
        'font_size' => 30,    // ← أو هذا
        'centered' => true,
    ],
    'course_name' => [
        'y' => 148,           // ← موضع اسم الدورة
        'font_size' => 16,
        'centered' => true,
    ],
    'date' => [
        'x' => 75,            // ← موضع التاريخ من اليسار
        'y_from_bottom' => 35, // ← من الأسفل
        'font_size' => 14,
        'centered' => false,
    ],
    'instructor' => [
        'x_from_right' => 70, // ← موضع المدرب من اليمين
        'y_from_bottom' => 35,
        'font_size' => 14,
        'centered' => false,
    ],
],
```

### فهم الأرقام:

#### للنصوص المتوسطة (اسم الطالب، اسم الدورة):
- **y**: المسافة من **أعلى** الصورة
- **زود الرقم** = النص ينزل ⬇️
- **قلل الرقم** = النص يطلع ⬆️

#### للنصوص السفلية (التاريخ، المدرب):
- **y_from_bottom**: المسافة من **أسفل** الصورة
- **زود الرقم** = النص يطلع ⬆️
- **قلل الرقم** = النص ينزل ⬇️

#### للمواضع الأفقية:
- **x**: المسافة من **اليسار**
- **x_from_right**: المسافة من **اليمين**

---

## اختبار سريع ⚡

### بعد التعديل:
1. احفظ `config.php`
2. افتح `test.html`
3. جرب توليد شهادة
4. شوف النتيجة

### إذا ما زال خطأ:
- جرب أرقام مختلفة
- استخدم `adjust_positions.html` للضبط التفاعلي

---

## نصائح مهمة 💡

### للحصول على أفضل نتيجة:

1. **حمّل الخط العربي أولاً**
   ```bash
   php download_font.php
   ```

2. **استخدم adjust_positions.html**
   أسهل وأسرع من التعديل اليدوي

3. **جرب بالتدريج**
   غيّر رقم واحد في كل مرة

4. **احفظ النسخة الأصلية**
   قبل التعديل، احتفظ بنسخة من `config.php`

5. **استخدم أرقام صحيحة**
   لا تستخدم أرقام عشرية

---

## أمثلة شائعة 📝

### مثال 1: اسم الطالب مرتفع جداً
```php
'student_name' => [
    'y' => 150,  // ← كان 105، زدناه لينزل
],
```

### مثال 2: اسم الدورة صغير جداً
```php
'course_name' => [
    'font_size' => 20,  // ← كان 16، زدناه
],
```

### مثال 3: التاريخ قريب من الحافة
```php
'date' => [
    'x' => 100,  // ← كان 75، زدناه ليبعد
],
```

### مثال 4: اسم المدرب خارج الإطار
```php
'instructor' => [
    'x_from_right' => 100,  // ← كان 70، زدناه
],
```

---

## الخطوات الكاملة (من البداية) 🎯

### 1. حمّل الخط
```bash
php download_font.php
```

### 2. افتح صفحة الضبط
```
http://localhost:8000/adjust_positions.html
```

### 3. اضبط المواضع
- عدّل الأرقام
- اضغط "تحديث المعاينة"
- كرر حتى تمام

### 4. احفظ الإعدادات
- اضغط "توليد الكود"
- انسخ الكود
- ضعه في `config.php`

### 5. اختبر
```
http://localhost:8000/test.html
```

---

## ما زلت تواجه مشاكل؟ 🆘

### تحقق من:
1. ✅ الخط موجود في `fonts/Cairo-Bold.ttf`
2. ✅ `config.php` محفوظ بدون أخطاء
3. ✅ الأرقام منطقية (لا تكون سالبة أو كبيرة جداً)
4. ✅ الصور موجودة في `templates/`

### جرب:
- `debug.php` للتحقق من كل شيء
- `adjust_positions.html` للضبط التفاعلي
- `test.html` للاختبار

---

**بالتوفيق! 🚀**
